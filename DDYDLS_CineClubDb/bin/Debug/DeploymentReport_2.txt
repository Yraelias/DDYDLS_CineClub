** Met en surbrillance
     Tables qui seront recréées avec les données migrées
       [dbo].[T_User]
     Index ordonnés en clusters à supprimer
       Aucun
     Index ordonnés en clusters à créer.
       Aucun
     Problèmes de données possibles
       La colonne [dbo].[T_User].[ID_UserRole] est en cours de suppression, des données risquent d'être perdues.
       La colonne [dbo].[T_User].[IsActive] de la table [dbo].[T_User] doit être ajoutée mais la colonne ne comporte pas de
         valeur par défaut et n'autorise pas les valeurs NULL. Si la table contient des données, le script ALTER ne fonctionnera
         pas. Pour éviter ce problème, vous devez ajouter une valeur par défaut à la colonne, la marquer comme autorisant les
         valeurs Null ou activer la génération de smart-defaults en tant qu'option de déploiement.
       La colonne [dbo].[T_User].[Registration_Date] de la table [dbo].[T_User] doit être ajoutée mais la colonne ne comporte
         pas de valeur par défaut et n'autorise pas les valeurs NULL. Si la table contient des données, le script ALTER ne
         fonctionnera pas. Pour éviter ce problème, vous devez ajouter une valeur par défaut à la colonne, la marquer comme
         autorisant les valeurs Null ou activer la génération de smart-defaults en tant qu'option de déploiement.

** Actions de l'utilisateur
     Supprimer
       [dbo].[T_User].[FK_2] (Index)
     Modifier
       [dbo].[T_Person] (Table)
     Recréer la table
       [dbo].[T_User] (Table)

** Actions de support
     Supprimer
       [dbo].[FK_9] (Clé étrangère)
     Créer
       [dbo].[FK_9] (Clé étrangère)

La colonne [dbo].[T_User].[ID_UserRole] est en cours de suppression, des données risquent d'être perdues.
La colonne [dbo].[T_User].[IsActive] de la table [dbo].[T_User] doit être ajoutée mais la colonne ne comporte pas de valeur par défaut et n'autorise pas les valeurs NULL. Si la table contient des données, le script ALTER ne fonctionnera pas. Pour éviter ce problème, vous devez ajouter une valeur par défaut à la colonne, la marquer comme autorisant les valeurs Null ou activer la génération de smart-defaults en tant qu'option de déploiement.
La colonne [dbo].[T_User].[Registration_Date] de la table [dbo].[T_User] doit être ajoutée mais la colonne ne comporte pas de valeur par défaut et n'autorise pas les valeurs NULL. Si la table contient des données, le script ALTER ne fonctionnera pas. Pour éviter ce problème, vous devez ajouter une valeur par défaut à la colonne, la marquer comme autorisant les valeurs Null ou activer la génération de smart-defaults en tant qu'option de déploiement.

